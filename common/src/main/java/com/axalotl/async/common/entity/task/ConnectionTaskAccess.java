package com.axalotl.async.common.entity.task;

/** A connection retains this resource when respawn replaces its player instance. */
public interface ConnectionTaskAccess {
    CooperativeTask tickweave$connectionTask();
}
